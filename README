pkginventory

This is a forked version of ody/pkginventory module, with newer Puppet version compatibility and other bugfixes.

For each installed package it creates a fact, e.g.: `pkg_ruby => 1:1.9.3.4`
